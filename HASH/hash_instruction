1)class Node
2)class List
class HashTable {
    List* table;
    int(*hash)(char*);
    .
    .
    .
}

функции:
    *поиск
    *вставка
    *удаление

структуры:
    node;
    list;

class HashTable {
    List* table;
    int(*hash)(char*);
    . . .
}
 гистограммы 5 штук
 y=collizium(количество элементов в конечном списке);
 x=index;

 сравнить эффективность
 среднее кол-во коллизий/среднеквадратичное кол-во коллизий
